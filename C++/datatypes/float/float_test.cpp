#include "iostream"
// C++ basic library

// using namespace std so that we will not have to put std:: everywhere
using namespace std;

// the function which will be executated
int main()
{
    // now we will create a float variable and name it myNum like this
    float myNum;

    // now we will asign value to it
    myNum = 80.12345;
    /* 
        User attention
        float type variables can only store till 7 decimal digits
    */

    // now print the number
    cout << myNum;
}
